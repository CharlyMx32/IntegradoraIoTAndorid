<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- Cambia el color según tu diseño -->
    <corners android:radius="20dp" /> <!-- Redondea las esquinas -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray" /> <!-- Borde opcional -->
</shape>
